// Declare your function first - call it "add"
function add(a, b) {
  return a + b;
}

// Call the function and assign to a variable `sum`
var sum = add(13, 124);
console.log(sum);

// ignore below

module.exports = { sum };
